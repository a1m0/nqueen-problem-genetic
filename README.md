# Solving NQueen Problem Using Genetic Algorithms

For running the programm, you need a c++ complier. Im using gnu compiler collection (g++).

### Running programm
In root project directory:
```
cd src
g++ main.cpp Board.cpp -o main
./main.exe
```
